{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build:webview",
            "type": "shell",
            "command": "pnpm -w build:webview && pnpm --filter @madlab/extension build"
        }
    ]
}